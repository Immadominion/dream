<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12 (API 31) and above: cloud backup + device-to-device transfer rules.
  allowBackup is set to false in the manifest, so cloud backup is disabled. These
  exclusions are defense-in-depth, ensuring auth tokens and credentials are never
  copied off-device even if backup/transfer is re-enabled later.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
